import {ActionsPropsType, SidebarPageType} from "./state";

const sidebarReducer = (state: SidebarPageType, action: ActionsPropsType): SidebarPageType => {
    switch (action.type) {
        default:
            return state
    }
}

export default sidebarReducer;